{
  "name": "flighttrace-scripts",
  "version": "1.0.0",
  "description": "Database initialization and management scripts",
  "scripts": {
    "setup": "node setupFirebase.js",
    "init-db": "node initializeFirebase.js",
    "create-collections": "node createCollections.js",
    "deploy-rules": "firebase deploy --only firestore:rules",
    "deploy-indexes": "firebase deploy --only firestore:indexes",
    "full-setup": "npm install && npm run setup && npm run deploy-rules && npm run deploy-indexes"
  },
  "dependencies": {
    "firebase-admin": "^11.11.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "firebase-tools": "^12.9.1"
  }
}